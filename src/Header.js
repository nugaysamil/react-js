import React from "react";

const Header = () => {
  const headerStyle = { background: "royalblue", color: "#fff" };

  return (
    <header style={headerStyle}>
      <h1>Grocceries List</h1>
    </header>
  );
};

export default Header;
